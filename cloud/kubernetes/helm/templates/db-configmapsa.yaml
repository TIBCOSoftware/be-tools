{{- if and (eq .Values.bsType "sharedAll" ) (eq .Values.omType "cache" ) (eq .Values.storeType "RDBMS" ) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "commonconfigmap.fullname" . }}
  labels:
    name: {{ include "commonconfigmap.fullname" . }}
{{ include "commonconfigmap.data" . }}
{{- end }}
