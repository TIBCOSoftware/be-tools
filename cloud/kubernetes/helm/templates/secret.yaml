{{- if .Values.certificates }}
apiVersion: v1
kind: Secret
metadata:
  name: "{{ .Release.Name }}-secret"
type: Opaque
data:
  {{- range $key, $val := .Values.certificates }}
  {{ $val.name }}: {{ $val.rawCertificate | b64enc }}
   {{- end }}
{{- end }}
