{{- if .Values.certSecrets }}
apiVersion: v1
kind: Secret
metadata:
  name: "{{ .Release.Name }}-secret"
type: Opaque
data:
  {{- range $key, $val := .Values.certSecrets }}
  {{ $val.name }}: {{ $val.pem | b64enc }}
   {{- end }}
{{- end }}
