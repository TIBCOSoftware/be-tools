{{- if or (eq .Values.omType "store" ) (eq .Values.omType "cache" ) }}
{{- if or (eq .Values.storeType "as4" ) (eq .Values.storeType "cassandra" ) (eq .Values.storeType "rdbms" ) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "configmapname.fullname" . }}
  labels:
    name: {{ include "configmapname.fullname" . }}
{{ include "configmap.data" . }}
{{- end }}
{{- end }}

